# Cron documenté dans docs/INGESTION-SCHEDULE.md (créneau isolé, décalé de 1
# jour vs ingest-maires.yaml pour éviter toute contention sur officials/mandates).
name: Ingest — Conseillers départementaux (RNE)
schedule: '0 22 2/5 * *'
env:
  - DATABASE_URL: ${DATABASE_URL}
steps:
  - name: git-pull
    command: git pull
  - name: enable-corepack
    command: corepack enable
    depends:
      - git-pull
  - name: install-dependencies
    command: yarn install --immutable
    depends:
      - enable-corepack
  - name: ingestion-conseillers-dep
    command: yarn --cwd packages/ingest ingest:conseillers-dep
    depends:
      - install-dependencies
