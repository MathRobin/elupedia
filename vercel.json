{
  "buildCommand": "cd ../.. && yarn db:migrate && cd packages/site && npx astro build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && corepack enable && yarn install",
  "framework": null
}
